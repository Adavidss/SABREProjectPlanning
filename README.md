# SABRE research overview

Published collaborator dashboard: https://adavidss.github.io/SABREProjectPlanning/

This repository contains only the generated public website. Source accounts and credentials are not connected here. The overview explicitly labels the published snapshot date; no research recap has been published yet.

The original Gantt is retained. Daily, weekly, previous-week and monthly recaps, historical date selection (90 days), and Markdown download work without a backend. Empty records are not claims that no research has happened.

To update: prepare reviewed shared content in the private SABRE authoring app, run `PYTHONPATH=src python3 -m sabre.static_export --output NEW_OUTPUT_FOLDER`, preview the generated folder, and replace this repository's generated files. Remove old generated files/ and recaps/ before copying, preserving .git and this README. Commit and push main. Do not upload private inputs, database files, credentials, or raw notes.

Pages publishing source: main branch, repository root. Intended custom domain: sabre-hyperpolarization.com once DNS is configured.
